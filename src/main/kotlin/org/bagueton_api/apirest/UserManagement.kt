package org.bagueton_api.apirest
/*

import org.bagueton_api.model.RecipeBean
import org.bagueton_api.model.UserBean
import org.bagueton_api.model.UserRepository
import org.bagueton_api.model.UserService
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RequestMapping
import org.springframework.web.bind.annotation.RestController

@RestController
@RequestMapping("/bagueton")
class UserManagement (val userService: UserService){

    @PostMapping("/createrecipe")

    fun createUser(username: String, password: String) {
        userService.createUser(username = username, password = password)
    }
}
*/
